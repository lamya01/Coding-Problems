#include <iostream>
#include <vector>
#include <strings>
using namespace std;

struct StringMeeting
{
    string s;
    string e;
    StringMeeting(string a, string b)
    {
        s = a, e = b;
    }
    StringMeeting()
    bool operator < (string s) 
    { 
        string l = stoi(this->)
        string r = stoi(s);
         
}